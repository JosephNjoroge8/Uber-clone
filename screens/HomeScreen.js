import { StyleSheet, Text, SafeAreaViewView } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-web'

const HomeScreen = () => {
  return (
    <SafeAreaViewView>
      <Text>welcome to our uber app clone</Text>
    </SafeAreaViewView>
  )
}

export default HomeScreen

const styles = StyleSheet.create({})